[pytest]
pythonpath = .
testpaths = tests
# e2e (Playwright) tests need a running app + browser; run them explicitly
# with `pytest tests/e2e`. Excluded from the default/unit run and from CI.
addopts = -q --ignore=tests/e2e
